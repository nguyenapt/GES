import { Injectable, Component } from '@angular/core';
import { RouterModule, Routes } from '@angular/router';

@Component({
    moduleId: '222',
    selector: 'my-app',
    templateUrl: "./claim-routing.html"
})

export class ClaimRoutingComponent {

}